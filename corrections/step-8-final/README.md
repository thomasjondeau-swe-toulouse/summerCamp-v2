# step-8-final — le **corrigé final**

**Contient :** l'app complète **+ le déploiement** (`render.yaml`, `frontend/src/api.js`, `DEPLOIEMENT.md`).

C'est le seul dossier qui n'est pas un point de départ : c'est l'arrivée de toute la semaine.
Ouvre-le pour comparer, pas pour travailler dedans.

**Le déploiement :** suis `DEPLOIEMENT.md` — mettre ton app en ligne gratuitement via GitHub + Render,
sans rien installer de plus.

## Lancer ce dossier

```bash
docker compose up
```

- L'app : http://localhost:5173
- L'API : http://localhost:8000/docs
